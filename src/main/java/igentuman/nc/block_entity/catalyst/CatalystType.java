package igentuman.nc.block_entity.catalyst;

/** Enumerates the catalyst categories a processor can support, each mapped to its own catalyst slot. */
public enum CatalystType {
    ENERGY,
    SPEED,
    EFFICIENCY
}
