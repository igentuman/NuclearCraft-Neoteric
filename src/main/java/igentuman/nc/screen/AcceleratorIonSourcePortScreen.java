package igentuman.nc.screen;

import igentuman.nc.container.MultiblockPortContainer;
import net.minecraft.network.chat.Component;
import net.minecraft.world.entity.player.Inventory;

public class AcceleratorIonSourcePortScreen extends MultiblockPortScreen {

    public AcceleratorIonSourcePortScreen(MultiblockPortContainer menu, Inventory inventory, Component title) {
        super(menu, inventory, title);
    }
}
